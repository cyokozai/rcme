services:
  python:
    container_name: python3.10
    image: python3.10:latest
    tty: true
    build: .
    working_dir: /root/project
    volumes:
      - ./workdir:/root/project
    networks:
      - default

networks:
  default:
    driver: bridge
